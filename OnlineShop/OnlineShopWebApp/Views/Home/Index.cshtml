@model List<Product>
@{
    ViewData["Title"] = "Home Page";
}

<div>
	@foreach (var product in Model)
	{
		<div>@product.Id</div>
		<div>@product.Name</div>
		<div>@product.Cost</div>
		<p></p>
	}
</div>
